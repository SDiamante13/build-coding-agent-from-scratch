node_modules/
dist/

.env
.env.local

logs/
*.log

# Authoring tooling. The sensors themselves are committed under sensors/;
# only the trigger is local, so an attendee's agent is never blocked mid-lesson.
.claude/settings.json
.claude/settings.local.json
sensors/reports/
CLAUDE.local.md

.DS_Store
.idea/
